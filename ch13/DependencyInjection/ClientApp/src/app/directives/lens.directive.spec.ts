import { LensDirective } from './lens.directive';

describe('LensDirective', () => {
  it('should create an instance', () => {
    const directive = new LensDirective();
    expect(directive).toBeTruthy();
  });
});
